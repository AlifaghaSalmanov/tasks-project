# Simple FastAPI stack that stays on the shared attendee network.

services:
  fastapi:
    build: .
    volumes:
      - ./:/app
    ports:
      - "4000:4000"
      - "5679:5679"
    environment:
      - DEBUGPY=1
      - DEBUGPY_WAIT_FOR_CLIENT=1
      - DEBUGPY_HOST=0.0.0.0
      - DEBUGPY_PORT=5679
    networks:
      - attendee_network

networks:
  attendee_network:
    external: true
    name: attendee_network
